import { EventRequiredDirective } from './event-required.directive';

describe('EventRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new EventRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
